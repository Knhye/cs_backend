name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: 3n1hye/cs-backend:latest

      - name: Deploy to EC2 (k3s)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo kubectl run prisma-migrate --image=3n1hye/cs-backend:latest --restart=Never --rm --attach \
              --env="DATABASE_URL=$(sudo kubectl get secret cs-secret -o jsonpath='{.data.DATABASE_URL}' | base64 -d)" \
              -- npx prisma migrate deploy
            sudo kubectl rollout restart deployment cs-deployment
            sudo kubectl rollout status deployment cs-deployment --timeout=120s
